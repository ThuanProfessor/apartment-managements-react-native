import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        flex: 1,
    
    },
    subject: {
        fontSize: 20,
        fontWeight: 'bold',
        marginBottom: 10,
        color: 'darkblue',
    },
});
